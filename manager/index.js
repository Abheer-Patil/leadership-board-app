const { BookRankingManager } = require("./bookManager");
const { DepartmentRankingManager } = require("./departmentManager");
const { LogManager } = require("./logManager");

module.exports = {
  DepartmentRankingManager,
  BookRankingManager,
  LogManager,
};
